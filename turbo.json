{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/**"]
    },
    "dev": {
      "outputs": ["dist/**"]
    },
    "test": {
      "outputs": []
    },
    "nextjs-cors#build": {
      "dependsOn": ["@next-cors/core#build"]
    }
  }
}
